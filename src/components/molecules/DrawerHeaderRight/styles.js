export const styles = (theme) => ({
  drawerHeaderRight: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    padding: '0 8px',
    ...theme.mixins.toolbar,
    backgroundColor: '#282828'
  },  
  Typography: {
    color: '#FFFFFF'
  }
});